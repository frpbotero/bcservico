// PWA Mobile — Fase 3
// Este módulo será implementado na Fase 3 com: login, lista de cautelas pendentes
// e coleta de assinatura digital via canvas touch.

import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <div style={{ padding: 24, fontFamily: "sans-serif" }}>
      <h1>App Cautelas — PWA</h1>
      <p>Módulo de assinatura digital — implementação na Fase 3.</p>
    </div>
  </React.StrictMode>
);
